# Makki Madni Travel - Backend

Node.js + Express API for travel packages and bookings.

## Setup

```bash
npm install
cp .env.example .env   # fill SUPABASE_DB_URL and PORT
npm run dev
```

Deploy to Vercel as a Node serverless function with `server.js` as entry.
